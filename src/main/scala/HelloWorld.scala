/*

//import scala.collection.immutable.Stack

/**
  * Created by kogentix on 07-06-2017.
  *
  *
  */

/*object HelloWorld extends Serializable {

  def main(args: Array[String]) {
    new opr_base_rtd_b_crew_onduty_processor().mainProcessor()
  }
}*/
/*
Class HelloWorld {
    val v = balancedOrNot("<>")
    println(v)
  }

    def balancedOrNot(symbolString: String): Unit = {
      val s = new scala.collection.mutable.Stack[String] // Stack()
      var balanced = true
      var index = 0
      // val Chk=(index < symbolString.length && balanced)
      while (index < symbolString.length && balanced) {
        val symbol = symbolString.charAt(index).toString
        //println(symbol)
        if (symbol == "<")
          s.push(symbol)
        else if (s.isEmpty)
          balanced = false
        else
          s.pop()
        index = index + 1

        if (balanced && s.isEmpty)
          return true
        else
          return false

      }
    }
*/

}

*/
